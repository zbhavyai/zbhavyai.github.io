import React from 'react';
import Header from '../components/Header';

const Home = () => {
  return (
    <React.Fragment>
      <Header activeNav='about' />
      Home page
    </React.Fragment>
  );
};

export default Home;
